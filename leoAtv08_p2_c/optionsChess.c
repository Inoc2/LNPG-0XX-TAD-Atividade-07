#include <stdio.h>

typedef struct optionsChess {
    char* colorPiece;
    char* typePiece;
    char* hierarchyPiece;
    char* positionPiece;
    char* newPositionPiece;
    char* positionOccupied;
} ;

char* movePiece(char* colorPiece, char* typePiece, char* positionPiece, char* newPositionPiece) {
    return NULL;
}

char* catchEnemyPiece(char* colorPiece, char* typePiece, char* positionPiece, char* newPositionPiece, char* positionOccupied, char* hierarchyPiece) {
    return NULL;
}

int canMovePiece(char* colorPiece, char* typePiece, char* positionPiece, char* newPositionPiece, char* positionOccupied) {
    return 0;
}

char* informationsPiece(char* colorPiece, char* typePiece, char* positionPiece, char* newPositionPiece, char* positionOccupied) {
    return NULL;
}