#include <stdio.h>

int main() {
    movePiece(NULL, NULL, NULL, NULL);
    catchEnemyPiece(NULL, NULL, NULL, NULL, NULL, NULL);
    canMovePiece(NULL, NULL, NULL, NULL, NULL);
    informationsPiece(NULL, NULL, NULL, NULL, NULL);
    
    return 0;
}